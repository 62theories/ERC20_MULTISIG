import { ethers } from "hardhat";
import { WisdomToken, WisdomToken__factory } from "../typechain";

async function deployWisdom() {
  // const WisdomToken = await ethers.getContractFactory("WisdomToken") as WisdomToken__factory;
  // const wisdomToken = await WisdomToken.deploy("Wisdom",
  // "WISDOM",
  // "WIS",
  // );
  //   const accounts = await hre.ethers.getSigners();
}
